OWNER DECISION — C3 duplicate-forward download-lock repair

Base: a898cf77913f786e69c2e85b357004972d796976
Protected files/functions:
- core/modules/net/include/net/download.h: struct dl_queued_key
- core/modules/net/src/download.c: qset_find, qset_insert_raw,
  qset_rebuild, dl_queue_push, dl_queue_blocks_class

Born-red evidence:
- C source bundle root e2755da6c709625fc945ff9f6471df71fcc5700d3682a98b0bc21f8c9edc95fa,
  wire SHA256 610b4aae55b023862d98b7d90b5d1104831cac3eb3094374a6b7727291bc575a.
  An already-forward 65536-entry duplicate batch held the download mutex for
  2022314 us; the paired history no-op took 10421 us.
- B registered download_contention RED: 20 rounds with 32768 queued forwards
  and 2048 duplicate forwards. Duplicate critical-work p95 150320 us; the
  first useful history enqueue p95 150609 us; all 20 rounds exceeded 100 ms.

Proposed change:
Cache the existing queue work class in its existing hash-membership entry.
An already-forward duplicate can then skip the history-promotion queue scan.
History-to-forward promotion retains the existing exact remove/reinsert path.
Applicable proposal SHA256:
a6af0343c0109c46c284bcf80107d868baa2e6147cc1728048a8d6c55cd58985

Invariants preserved:
- queue array remains the source of truth;
- no queue or in-flight cap changes;
- forward-before-history ordering unchanged;
- peer avoidance and generation semantics unchanged;
- duplicate enqueue adds zero and credits no receipt;
- history-to-forward promotion occurs exactly once;
- allocation remains bounded by the existing qset and queue caps;
- no consensus predicate, validation, custody, peer permission, or wire rule
  changes.

Required regression/proof:
- registered duplicate-forward contention RED to GREEN with useful history
  enqueue no longer serialized behind the full scan;
- existing download group: dedup, promotion, caps, forward priority, queue
  accounting, avoidance, timeout/disconnect settlement;
- build, lint, core seal, exact affected native proof;
- loaded backfill rate, lock timing and foreground command p95;
- independent B review and reproduction before native landing under HOLD.

Owner action required by repository policy:
make core-unseal REASON="C3: eliminate O(batch*queue) duplicate-forward scan while preserving exact download policy and accounting"
